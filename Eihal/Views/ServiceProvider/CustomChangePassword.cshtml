@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    string cultureCode = System.Threading.Thread.CurrentThread.CurrentCulture.Name;
    string direction = cultureCode.StartsWith("ar", StringComparison.OrdinalIgnoreCase) ? "rtl" : "ltr";
    string textAlign = direction == "rtl" ? "right" : "left";
    bool isEng = direction == "ltr" ? true : false;
}


<div id="page-top">

    <!-- Page Wrapper -->
    <div id="wrapper">

        <!-- Sidebar -->
        <!-- End of Sidebar -->
        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">

            <!-- Main Content -->
            <div id="content">

                <!-- Topbar -->
                <!-- End of Topbar -->
                <!-- Begin Page Content -->
                <div class="container-fluid" style="direction:@direction;text-align:@textAlign">
                    <!-- Page Heading -->
                    <div class="d-sm-flex align-items-center justify-content-between mb-4" style="margin-top:125px;">
                        @*  <a href="#" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                        <i class="fas fa-download fa-sm text-white-50"></i> Generate Report
                        </a>*@
                    </div>
                    <!-- Content Row -->


                    <div class="row">
                        @Html.Partial("_Sidebar")
                        <div class="col-xl-8 col-lg-8 ">

                            <div id="profile-status-container" style="color:white"></div>

                            <br />
                            <div class="card shadow mb-4">
                                <!-- Card Header - Dropdown -->
                                <div class="tab-container card-header py-2 d-flex flex-row align-items-center justify-content-between">

                                    <h6 style="color:#00768c" class="m-0 font-weight-bold text-primary">
                                        <a style="color:#00768c" class="nav-link" href="#">
                                            <i style="color:#00768c" class="fa fa-key" aria-hidden="true"></i>
                                            <span style="color:#00768c">Change Your Password</span>
                                        </a>
                                    </h6>

                                </div>
                                <!-- Card Body -->
                                <div class="card-body" style="max-height: 768px; overflow-y: auto;">

                                    <!-- InComing Tab -->
                                    <div id="tab1" class="tab-content">
                                        <div class="row">
                                            <div class="col-md-6">
                                                    <div class="form-floating">
                                                        <label style="font-weight:bold;color:#00768c" class="form-label">Old Password</label>
                                                        <input id="oldPass-input" class="form-control" autocomplete="current-password" aria-required="true" />
                                                    </div>
                                                    <div class="form-floating">
                                                        <label style="font-weight:bold;color:#00768c" class="form-label">New Password</label>
                                                        <input id="newPass-input" class="form-control" autocomplete="new-password" aria-required="true" />
                                                    </div>
                                                    <div class="form-floating">
                                                        <label style="font-weight:bold;color:#00768c" class="form-label">Confirm New Password</label>
                                                        <input id="confirmPass-input" class="form-control" autocomplete="new-password" aria-required="true" />
                                                    </div>
                                                    <br />
                                                    <button onclick="updatePassword()" class="w-40 btn btn-md btn-primary">Update password</button>
                                            </div>
                                        </div>
                                    </div>



                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.container-fluid -->

        </div>
        <!-- End of Main Content -->
        <!-- Footer -->
        <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->
</div>



@section Scripts {
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function () {
            var currentUrl = window.location.href;
            if (currentUrl.includes("Notifications")) {
                $("#myServices-li").removeClass("activeLink");
                $("#myServices-text").removeClass("activeLink");

                $("#myProfile-li").removeClass("activeLink");
                $("#myProfile-text").removeClass("activeLink");

                $("#myReferrals-li").removeClass("activeLink");
                $("#myReferrals-text").removeClass("activeLink");

                $("#notifications-li").addClass("activeLink");
                $("#notifications-text").addClass("activeLink");
            } else {
                console.log("The URL does not contain 'Notifications'.");
            }

        })

        function updatePassword() {
            // Get the values from the form
            var oldPassword = $("#oldPass-input").val();
            var newPassword = $("#newPass-input").val();

            // Create an object with the data
            var data = {
                oldPassword: oldPassword,
                newPassword: newPassword
            };

            // Make an AJAX request to the UpdatePassword action
            $.ajax({
                type: "POST",
                url: "/ServiceProvider/UpdatePassword", 
                data: data,
                success: function (response) {
                    // Display success message
                    $("#result").html(response.message);
                },
                error: function (error) {
                    // Display error message
                    $("#result").html("Error: " + error.responseJSON.error);
                }
            });
        }


    </script>

}

<style>

    .activeLink {
        color: #00768c;
        font-weight: bold;
        pointer-events: none;
        cursor: not-allowed;
        background-color: whitesmoke
    }

</style>